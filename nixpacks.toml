[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x", "openssl", "python3", "gcc", "gnumake", "pkg-config", "libusb1", "libudev"]

[phases.install]
cmds = ["pnpm i --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run deploy:build"]

[start]
cmd = "pnpm run deploy:start"