{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/:path*"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "buildCommand": "cd ../.. && pnpm install && pnpm --filter @tempwallet/shared run build && cd apps/frontend && pnpm run build",
    "installCommand": "cd ../.. && pnpm install",
    "framework": "vite",
    "outputDirectory": "dist"
}